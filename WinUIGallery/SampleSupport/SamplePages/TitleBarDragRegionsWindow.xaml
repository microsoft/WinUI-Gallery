<?xml version="1.0" encoding="utf-8" ?>
<Window
    x:Class="WinUIGallery.Samples.SamplePages.TitleBarDragRegionsWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:WinUIGallery.Samples.SamplePages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="TitleBar drag regions sample"
    mc:Ignorable="d">
    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TitleBar
            x:Name="titleBar"
            Title="Drag regions"
            Subtitle="Try dragging the window">
            <TitleBar.Resources>
                <!--  Make TitleBar.Content stretch to fill the available space.  -->
                <HorizontalAlignment x:Key="TitleBarContentHorizontalAlignment">Stretch</HorizontalAlignment>
            </TitleBar.Resources>
            <TitleBar.IconSource>
                <ImageIconSource ImageSource="/Assets/Tiles/GalleryIcon.ico" />
            </TitleBar.IconSource>
            <TitleBar.Content>
                <Grid ColumnSpacing="8" HorizontalAlignment="Stretch">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <!--  Interactive control: auto-excluded from drag.  -->
                    <AutoSuggestBox
                        MaxWidth="580"
                        HorizontalAlignment="Stretch"
                        VerticalAlignment="Center"
                        PlaceholderText="Search..."
                        QueryIcon="Find" />
                    <!--  Right-side container. Extra elements added at runtime are
                          inserted here so they don't collide with the status badge.  -->
                    <StackPanel
                        x:Name="RightHeaderPanel"
                        Grid.Column="1"
                        Orientation="Horizontal"
                        Spacing="8"
                        VerticalAlignment="Center">
                        <!--  Interactive Button. Its drag behavior is controlled via
                              the TitleBar.IsDragRegion attached property below.  -->
                        <Button
                            x:Name="StatusBadge"
                            VerticalAlignment="Center"
                            Click="StatusBadge_Click"
                            Content="Status"
                            Style="{StaticResource AccentButtonStyle}" />
                    </StackPanel>
                </Grid>
            </TitleBar.Content>
        </TitleBar>

        <ScrollViewer Grid.Row="1" Padding="32,24">
            <StackPanel MaxWidth="640" Spacing="16">
                <TextBlock
                    Style="{ThemeResource SubtitleTextBlockStyle}"
                    Text="Custom drag regions" />
                <TextBlock TextWrapping="WrapWholeWords">
                    Try dragging the window from different parts of the title bar.
                    Interactive controls (like the search box) are automatically
                    excluded from the drag region by the new default behavior in
                    Windows App SDK 2.1.
                </TextBlock>

                <TextBlock
                    Style="{ThemeResource BodyStrongTextBlockStyle}"
                    Text="Status badge: TitleBar.IsDragRegion" />
                <TextBlock
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    Text="Pick a value for the badge in the title bar."
                    TextWrapping="WrapWholeWords" />
                <RadioButtons
                    x:Name="BadgeIsDragRegionRadios"
                    SelectedIndex="0"
                    SelectionChanged="BadgeIsDragRegionRadios_SelectionChanged">
                    <x:String>Unset (framework decides — clickable, since Button is interactive)</x:String>
                    <x:String>True (always draggable — overrides the framework default)</x:String>
                    <x:String>False (always clickable)</x:String>
                </RadioButtons>

                <TextBlock
                    Style="{ThemeResource BodyStrongTextBlockStyle}"
                    Text="Dynamic content" />
                <TextBlock
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    Text="When you add or remove elements in TitleBar.Content at runtime, call RecomputeDragRegions() to refresh."
                    TextWrapping="WrapWholeWords" />
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <Button Click="ToggleExtraButton_Click" Content="Toggle extra title bar button" />
                    <Button Click="RecomputeDragRegions_Click" Content="RecomputeDragRegions()" />
                </StackPanel>

                <TextBlock
                    x:Name="StatusText"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                    Text=""
                    TextWrapping="WrapWholeWords" />
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>
