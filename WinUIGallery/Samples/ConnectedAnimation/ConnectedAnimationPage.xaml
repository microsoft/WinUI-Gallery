<Page
    x:Class="WinUIGallery.ControlPages.ConnectedAnimationPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:WinUIGallery.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    x:Name="pageRoot"
    mc:Ignorable="d">
    <StackPanel>

        <controls:ControlExample
            SampleDefinition="ConnectedAnimation\ConnectedAnimationListPage.txt">
            <Frame
                x:Name="CollectionContentFrame"
                Height="750"
                MinWidth="500" />
        </controls:ControlExample>

        <controls:ControlExample
            SampleDefinition="ConnectedAnimation\ConnectedAnimationElementsSame.txt">
            <Frame
                x:Name="CardFrame"
                MinWidth="500"
                MinHeight="300"
                MaxHeight="700" />
        </controls:ControlExample>

        <controls:ControlExample SampleDefinition="ConnectedAnimation\SimpleConnectedAnimation.txt">
            <controls:ControlExample.Options>
                <StackPanel>
                    <Button
                        HorizontalAlignment="Stretch"
                        Click="NavigateButton_Click"
                        Content="Navigate" />

                    <TextBlock
                        Margin="0,6"
                        Style="{ThemeResource BaseTextBlockStyle}"
                        Text="Configurations" />
                    <RadioButtons x:Name="ConfigurationPanel" SelectedIndex="0">
                        <RadioButton Content="Default" />
                        <RadioButton Content="Gravity" />
                        <RadioButton Content="Direct" />
                        <RadioButton Content="Basic" />
                    </RadioButtons>
                </StackPanel>
            </controls:ControlExample.Options>
            <Frame
                x:Name="ContentFrame"
                Height="500"
                MinWidth="500"
                MinHeight="300" />
        </controls:ControlExample>

        <controls:ControlExample
            SampleDefinition="ConnectedAnimation\ConnectedAnimationItemsrepeater.txt">
            <StackPanel>
                <TextBlock Margin="0,0,0,12" TextWrapping="WrapWholeWords">
                    Unlike ListView and GridView, ItemsRepeater does not have built-in ConnectedAnimation methods.
                    Use ConnectedAnimationService.PrepareToAnimate() directly, and manually find the target element in the visual tree.
                    Click an item to navigate with a connected animation.
                </TextBlock>
                <Frame
                    x:Name="ItemsRepeaterFrame"
                    Height="400"
                    MinWidth="500" />
            </StackPanel>
        </controls:ControlExample>
    </StackPanel>
</Page>
