<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="WinUIGallery.ControlPages.JumpListPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:WinUIGallery.Controls"
    mc:Ignorable="d">

    <StackPanel>
        <InfoBar Margin="0,10,0,0" IsOpen="True" IsClosable="False" Severity="Warning"
                 Title="JumpList is not available in unpackaged mode."
                 Message="This API requires the app to be running in packaged mode (MSIX)."/>

        <controls:ControlExample HeaderText="Adding tasks to the jump list">
            <StackPanel Spacing="8">
                <TextBlock TextWrapping="Wrap"
                           Text="Tasks appear in the default group at the bottom of the jump list. They launch the app with specific arguments." />
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <Button Content="Add sample tasks"
                            Style="{StaticResource AccentButtonStyle}"
                            Click="AddTasksButton_Click" />
                    <Button Content="Clear all items"
                            Click="ClearTasksButton_Click" />
                </StackPanel>
            </StackPanel>
            <controls:ControlExample.CSharp>
                <x:String xml:space="preserve">
var jumpList = await JumpList.LoadCurrentAsync();

var task = JumpListItem.CreateWithArguments("/compose", "New Message");
task.Description = "Compose a new message";
task.Logo = new Uri("ms-appx:///Assets/Square44x44Logo.scale-200.png");

jumpList.Items.Add(task);
await jumpList.SaveAsync();
                </x:String>
            </controls:ControlExample.CSharp>
        </controls:ControlExample>

        <controls:ControlExample HeaderText="Adding items to a custom group">
            <StackPanel Spacing="8">
                <TextBlock TextWrapping="Wrap"
                           Text="Items with a non-empty GroupName are placed together in a named section in the jump list." />
                <Button Content="Add custom group items"
                        Click="AddCustomGroupButton_Click" />
            </StackPanel>
            <controls:ControlExample.CSharp>
                <x:String xml:space="preserve">
var jumpList = await JumpList.LoadCurrentAsync();

var item = JumpListItem.CreateWithArguments("/project-alpha", "Project Alpha");
item.GroupName = "Projects";
item.Description = "Open Project Alpha";
item.Logo = new Uri("ms-appx:///Assets/Square44x44Logo.scale-200.png");

jumpList.Items.Add(item);
await jumpList.SaveAsync();
                </x:String>
            </controls:ControlExample.CSharp>
        </controls:ControlExample>

        <controls:ControlExample HeaderText="Current jump list items">
            <StackPanel Spacing="8">
                <TextBlock TextWrapping="Wrap"
                           Text="Right-click the app icon in the taskbar to see the jump list. The items below reflect the current custom entries." />
                <Button Content="Refresh"
                        Click="RefreshButton_Click" />
                <ListView x:Name="JumpListItemsView"
                          MaxHeight="300"
                          SelectionMode="None">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid Padding="4" ColumnSpacing="12">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <FontIcon Glyph="&#xE71E;" FontSize="16" VerticalAlignment="Center" />
                                <StackPanel Grid.Column="1">
                                    <TextBlock Text="{Binding DisplayName}" FontWeight="SemiBold" />
                                    <TextBlock Text="{Binding Arguments}"
                                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                               FontSize="12" />
                                </StackPanel>
                                <TextBlock Grid.Column="2" Text="{Binding GroupName}"
                                           Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                                           VerticalAlignment="Center" FontSize="12" />
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
                <TextBlock x:Name="EmptyListText"
                           Text="No custom items in the jump list."
                           Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                           Visibility="Visible" />
            </StackPanel>
        </controls:ControlExample>
    </StackPanel>
</Page>