<!-- Copyright (c) Microsoft Corporation and Contributors. -->
<!-- Licensed under the MIT License. -->

<UserControl
    x:Class="WinUIGallery.DesktopWap.Controls.DesignGuidance.ColorPageExample"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel Padding="16" CornerRadius="4"
        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1"
        Background="{x:Bind Background, Mode=OneWay}">
        <TextBlock Text="Example" FontWeight="Bold"/>
        <TextBlock Text="{x:Bind Text, Mode=OneWay}" Margin="0,4,0,0"/>
        <ContentPresenter Content="{x:Bind ExampleContent, Mode=OneWay}" HorizontalAlignment="Center"/>
    </StackPanel>
</UserControl>
