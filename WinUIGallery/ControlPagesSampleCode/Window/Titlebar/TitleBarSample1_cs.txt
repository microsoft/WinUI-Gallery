Window window = new(); // This is the window that will be have a custom title bar
window.ExtendsContentIntoTitleBar = true;  // Enable the custom titlebar
window.SetTitleBar(AppTitleBar);  // Set the UIElement that will act as titlebar